<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.realcapstone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CompareMenu2"></activity>
        <activity android:name=".InterviewMenu" />
        <activity android:name=".Fail" />
        <activity android:name=".Pass" />
        <activity android:name=".InputSpec2" />
        <activity android:name=".Comparing" />
        <activity android:name=".CompareMenu" />
        <activity android:name=".InputSpec" />
        <activity android:name=".Register" />
        <activity android:name=".MainMenu" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Startpage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>